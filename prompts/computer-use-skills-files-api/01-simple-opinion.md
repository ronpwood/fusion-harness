Computer use, the Skills API, and the Files API are now generally available on the Claude Platform. GA additions: multi-action turns (several tool actions per model turn instead of one) and HIPAA-BAA eligibility for computer use; a new browser use tool that reads page structure instead of acting on raw screenshot pixels; a simpler Skills API for uploading and versioning skills that run in Claude's code-execution sandbox with nothing to host; and Files API upgrades — automatic file expiration, 5x higher rate limits, 1 TB storage per organization. Also available on Microsoft Foundry today, with Vertex AI support coming soon; existing beta integrations keep working during migration.

Read-only task: choose the ONE MOST IMPORTANT capability a team building a production support/ops agent should test first. Give:

1. your choice and why it has the highest immediate leverage;
2. a 15-minute experiment with concrete API calls or steps;
3. one GA-migration risk or beta-compatibility check specific to that capability;
4. the observable result that would change your recommendation.

FIRST read ai_docs/computer-use-skills-files-api.md.
